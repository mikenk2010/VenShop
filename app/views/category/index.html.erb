<div class="container-home">
  <div class="items">
    <div class="clear" />
    <div class="newest">
      <div class="headline-newest">List all products - Cate</div>
<%= will_paginate @category %>
     <% @category.each do |product| %>
      <div class="list-Browse-NewsRv">
        <div class="bl-image"><a href="/item">
          <img src="<%= product.img %>" />
        </a>
        </div>
        <div class="price"><%= product.price %> Y
          <div class="newest-datetime"><%= product.date %>


          </div>
        </div>
        <div class="title-Br-NewsRv"><a href="/products/<%= product.id %>"><%= product.title %></a>
        </div>
        <div class="Br-NewsRv-cont">
          <%= product.desc %>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
<div class="clear"></div>

<div class="clear"></div>
